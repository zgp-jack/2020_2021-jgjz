<block wx:if="{{$taroCompReady}}">
    <view class="feedback">
        <view class="heard">
            <view>
                <view class="heard-title">为了提高沟通效率，建议您添加</view>
                <view>平台微信:<text class="blued">{{PHONE}}</text>
                </view>
            </view>
            <view class="copy" bindtap="anonymousFunc0">点击复制</view>
        </view>
        <view class="evaluate">
            <view>您觉得记工记账用起来怎么样？给个评价吧~</view>
            <view class="evaluateRadio">
                <radio-group class="RadioGroup">
                    <radio wx:key="id" class="evaluateRadioList" bindtap="anonymousFunc1" color="#0099FF" wx:for="{{loopArray34}}" wx:for-item="v" wx:for-index="__index1" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey}}">{{v.$original.title}}</radio>
                </radio-group>
            </view>
        </view>
        <view class="opinion">
            <textarea class="opinionTextarea" bindinput="anonymousFunc2" placeholder="请留下您的意见或建议" placeholder-style="color:rgba(167, 167, 167, 1)"></textarea>
            <view class="image">
                <image-view compid="{{$compid__42}}"></image-view>
            </view>
            <view class="clear"></view>
            <block wx:if="{{image.item.length === 0}}">
                <view class="addImage">添加图片</view>
            </block>
            <view class="tips">注：如果反馈意见被采纳，平台将赠送您2~10个鱼泡网积分</view>
        </view>
        <view class="submitBtnBox">
            <view class="submitBtn" bindtap="anonymousFunc3">提交</view>
        </view>
    </view>
</block>