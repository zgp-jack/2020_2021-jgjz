<block wx:if="{{$taroCompReady}}">
    <block>
        <view class="foreman">
            <view>
                <view class="tabber">
                    <view class="tabber-list" wx:key="id" bindtap="anonymousFunc0" wx:for="{{loopArray81}}" wx:for-item="v" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey}}">
                        <view class="{{v.$original.click ? 'tabber-list-box' : ''}}">
                            <view class="{{v.$loopState__temp2}}">
                                <block wx:if="{{v.$original.id === 1}}">
                                    <image src="{{IMGCDNURL + 'dian.png'}}" class="tabber-list-image-dian-img"></image>
                                </block>
                                <block wx:if="{{v.$original.id === 2}}">
                                    <image src="{{IMGCDNURL + 'bao.png'}}" class="tabber-list-image-bao-img"></image>
                                </block>
                                <block wx:if="{{v.$original.id === 3}}">
                                    <image src="{{IMGCDNURL + 'jie.png'}}" class="tabber-list-image-jie-img"></image>
                                </block>
                            </view>
                            <view>{{v.$original.name}}</view>
                        </view>
                    </view>
                </view>
                <block wx:if="{{recorderType === 2}}">
                    <view class="contractor">
                        <view>包工类型</view>
                        <view class="radioList">
                            <radio-group>
                                <radio color="#0099FF" class="borrowing-Radio-list" checked="{{v.$original.click}}" wx:key="id" bindtap="anonymousFunc1" wx:for="{{loopArray82}}" wx:for-item="v" wx:for-index="__index1" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey2}}">{{v.$original.name}}</radio>
                            </radio-group>
                        </view>
                    </view>
                </block>
            </view>
            <view class="projectName">
                <view class="publish-recruit-card">
                    <view class="publish-list-item" bindtap="anonymousFunc2"><text class="pulish-list-title">项目名称</text>:
                        <input class="publish-list-input" type="text" disabled="{{true}}" placeholder="请选择项目名称" value="{{model && model.name}}" />
                    </view>
                </view>
                <block wx:if="{{identity === 2}}">
                    <view class="publish-recruit-card">
                        <view class="publish-list-item" bindtap="anonymousFunc3"><text class="pulish-list-title">班组长</text>:
                            <input class="publish-list-input" type="text" disabled="{{true}}" placeholder="请选择班组长" value="{{foremanTitle}}" />
                        </view>
                    </view>
                </block>
                <block wx:if="{{identity === 1}}">
                    <view class="workerBox">
                        <view class="workerBox-list">
                            <view class="workerBox-list-title">
                                <view>选择工人</view>
                                <view class="workerBox-list-title-origin-box">
                                    <view class="workerBox-list-title-origin">工资如有变动，请长按头像修改</view>
                                    <view class="workerBox-list-title-origin-tips"><text class="workerBox-list-title-origin-color"></text>表示当天已记过工</view>
                                </view>
                            </view>
                            <view>
                                <view class="whole" catchtap="anonymousFunc4">全选</view>
                            </view>
                        </view>
                        <view class="workerBox-change">
                            <view></view>
                            <view>(已选{{clickNum}}人)</view>
                        </view>
                        <view class="workerItem">
                            <view class="listPosition" bindtap="anonymousFunc5" wx:for="{{loopArray83}}" wx:for-item="v" wx:for-index="__index5" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey3}}">
                                <block wx:if="{{v.$original.id === 1}}">
                                    <view>
                                        <view class="{{v.$original.click ? 'workerItem-list-first-click' : 'workerItem-list-first'}}">{{v.$loopState__temp4}}</view>
                                        <view class="workerItem-list-title">{{v.$original.name}}</view>
                                    </view>
                                </block>
                                <block wx:if="{{v.$original.id !== 1}}">
                                    <view bindlongclick="anonymousFunc6" data-e-longclick-so="this" data-e-longclick-a-a="{{v._$indexKey4}}">
                                        <view class="{{v.$original.click ? 'workerItem-list-click' : 'workerItem-list'}}">{{v.$original.name}}</view>
                                        <block wx:if="{{!v.$original.del && !v.$original.set}}">
                                            <view class="workerItem-list-icon" catchtap="anonymousFunc7" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey5}}">
                                                <image class="workerItem-list-icon-img" src="{{IMGCDNURL + 'mark.png'}}"></image>
                                            </view>
                                        </block>
                                        <block wx:if="{{v.$original.del}}">
                                            <view class="workerItem-list-icon-del" catchtap="anonymousFunc8" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey6}}">
                                                <image src="{{IMGCDNURL + 'reduce.png'}}" class="workerItem-list-icon-del-img"></image>
                                            </view>
                                        </block>
                                        <view class="workerItem-list-title">{{v.$original.name}}</view>
                                    </view>
                                </block>
                            </view>
                            <block wx:if="{{delType}}">
                                <view>
                                    <view class="ml20" bindtap="anonymousFunc9">
                                        <view class="workerItem-ok">
                                            <image src="{{IMGCDNURL + 'ok.png'}}" class="workerItem-ok-img"></image>
                                        </view>
                                        <view class="workerItem-list-title">完成</view>
                                    </view>
                                </view>
                            </block>
                            <block wx:if="{{!delType}}">
                                <view class="flex">
                                    <view class="listPosition" bindtap="anonymousFunc10">
                                        <view class="workerItem-add">+</view>
                                        <view class="workerItem-list-title">添加</view>
                                    </view>
                                    <view class="listPosition" bindtap="anonymousFunc11">
                                        <view class="workerItem-del">-</view>
                                        <view class="workerItem-list-title">删除</view>
                                    </view>
                                </view>
                            </block>
                        </view>
                    </view>
                </block>
            </view>
            <block wx:if="{{recorderType === 1 || recorderType === 2 && contractor === 0}}">
                <view class="publish-recruit-card">
                    <view class="publish-list-item" bindtap="anonymousFunc12"><text class="pulish-list-title">上班时长</text>:
                        <input class="publish-list-input" type="text" disabled="{{true}}" placeholder="请选择上班时长" value="{{model && model.duration}}" />
                    </view>
                </view>
            </block>
            <block wx:if="{{identity === 2 && (recorderType === 1 || recorderType === 1 || recorderType === 2 && contractor === 0)}}">
                <view>
                    <view class="publish-recruit-card-money" bindtap="anonymousFunc13">
                        <view class="publish-list-item-money">
                            <view class="pulish-list-title-money">
                                <view>我的工钱
                                    <view class="mt10">(自动计算)</view>
                                </view>
                            </view>
                            <input class="publish-list-input-money" type="text" disabled="{{true}}" placeholder="请选择工钱" value="{{model && model.workersWages}}" />
                        </view>
                    </view>
                </view>
            </block>
            <block wx:if="{{recorderType === 2 && contractor === 1}}">
                <view class="measure">
                    <view class="publish-recruit-card">
                        <view class="publish-list-item"><text class="pulish-list-title">工程量</text>:
                            <input class="publish-list-input-amount" type="number" placeholder="请填写工程量" bindinput="anonymousFunc14" value="{{model && model.amount}}" />
                            <view class="amountType" bindtap="anonymousFunc15">{{unit}}</view>
                        </view>
                    </view>
                    <view class="publish-recruit-card">
                        <view class="publish-list-item"><text class="pulish-list-title">单价</text>:
                            <input class="publish-list-input" type="number" placeholder="请填写单价" bindinput="anonymousFunc16" value="{{model && model.price}}" />
                        </view>
                    </view>
                    <view class="publish-recruit-card">
                        <view class="publish-list-item"><text class="pulish-list-title">工钱</text>:
                            <input class="publish-list-input" type="number" bindinput="anonymousFunc17" placeholder="工程量和单价未知时，可直接填写" value="{{model && model.wages}}" />
                        </view>
                    </view>
                </view>
            </block>
            <block wx:if="{{recorderType === 3}}">
                <view class="borrowing">
                    <view class="publish-recruit-card">
                        <view class="publish-list-item"><text class="pulish-list-title">本次借支</text>:
                            <input class="publish-list-input" type="number" bindinput="anonymousFunc18" placeholder="请输入本次借支金额" value="{{model && model.borrowing}}" />
                        </view>
                    </view>
                    <view class="publish-recruit-card">
                        <view class="borrowing-publish-list-item"><text class="borrowing-pulish-list-title">本次借支属于(可不选)</text>
                        </view>
                        <view>
                            <radio-group class="borrowing-Radio">
                                <radio bindtap="anonymousFunc19" class="borrowing-Radio-list" color="#0099FF" checked="{{v.$original.click}}" wx:key="id" wx:for="{{loopArray84}}" wx:for-item="v" wx:for-index="__index19" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey7}}">{{v.$original.name}}</radio>
                            </radio-group>
                        </view>
                    </view>
                </view>
            </block>
            <view class="newTime">
                <view class="publish-recruit-card" bindtap="anonymousFunc20">
                    <view class="publish-list-item"><text class="pulish-list-title">日期</text>:
                        <input class="publish-list-input" type="text" disabled="{{true}}" bindtap="anonymousFunc21" placeholder="请选择日期" value="{{model && model.time}}" />
                    </view>
                </view>
            </view>
            <view class="textareaList">
                <view class="publish-recruit-card">
                    <view class="publish-list-ditals">
                        <view>备注</view>
                        <textarea class="textarea" placeholder="请填写备注..." value="{{model && model.details}}" bindinput="anonymousFunc22" maxlength="{{500}}"></textarea>
                    </view>
                    <view>
                        <words-total compid="{{$compid__120}}"></words-total>
                    </view>
                </view>
                <view class="image">
                    <image-view compid="{{$compid__121}}"></image-view>
                </view>
            </view>
            <view class="clear"></view>
            <view class="foreman-footer">
                <view class="footer-left" bindtap="anonymousFunc23">保存再记录一笔</view>
                <view class="footer-right" bindtap="anonymousFunc24">保存</view>
            </view>
            <project-modal compid="{{$compid__122}}"></project-modal>
            <recorder-popup compid="{{$compid__123}}"></recorder-popup>
            <quantities compid="{{$compid__124}}"></quantities>
            <work-overtime compid="{{$compid__125}}"></work-overtime>
            <working-hours compid="{{$compid__126}}"></working-hours>
            <create-project compid="{{$compid__127}}"></create-project>
            <calendar-modal compid="{{$compid__128}}"></calendar-modal>
            <wage-standard compid="{{$compid__129}}"></wage-standard>
            <add-member compid="{{$compid__130}}"></add-member>
            <wages-modal compid="{{$compid__131}}"></wages-modal>
            <edit-project compid="{{$compid__132}}"></edit-project>
            <at-drawer compid="{{$compid__133}}">
                <view class="atDrawer-box">
                    <view class="atDrawer-heard">
                        <view bindtap="anonymousFunc26">
                            <image src="{{IMGCDNURL + 'projectLeft.png'}}" class="addIcon"></image>
                        </view>
                        <view>项目名称</view>
                        <block wx:if="{{!edit}}">
                            <view class="atDrawer-heard-edit" bindtap="anonymousFunc27">修改/删除</view>
                        </block>
                        <block wx:if="{{edit}}">
                            <view class="atDrawer-heard-edit" bindtap="anonymousFunc28">取消</view>
                        </block>
                    </view>
                    <view class="atDrawer-heard-content">
                        <view class="atDrawer-list" bindtap="anonymousFunc29" wx:for="{{loopArray85}}" wx:for-item="v" wx:for-index="__index29" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey8}}">
                            <view class="atDrawer-list-title"><text wx:for="{{v.$original.child}}" wx:for-item="val" wx:for-index="_anonIdx2">{{v.$original.name}}</text>
                            </view>
                            <view class="atDrawer-list-flex">
                                <view></view>
                                <view>
                                    <block wx:if="{{!edit}}">
                                        <view>
                                            <checkbox class="checkbox" color="#0099FF" value="" catchtap="anonymousFunc30" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey9}}"></checkbox>
                                        </view>
                                    </block>
                                    <block wx:if="{{edit}}">
                                        <view class="atDrawer-list-flex">
                                            <view class="atDrawer-list-flex-btn" catchtap="anonymousFunc31" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey10}}">修改</view>
                                            <view class="atDrawer-list-flex-btn" catchtap="anonymousFunc32" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey11}}">删除</view>
                                        </view>
                                    </block>
                                </view>
                            </view>
                        </view>
                    </view>
                    <view class="atDrawer-footer">
                        <view class="atDrawer-footer-btn" bindtap="anonymousFunc33">创建项目</view>
                    </view>
                </view>
            </at-drawer>
        </view>
    </block>
</block>