<block wx:if="{{$taroCompReady}}">
    <view>
        <block wx:if="{{display}}">
            <view class="quantities-complaintModal">
                <view class="quantities-complaintModal-content">
                    <view class="quantities-complaintModal-content-title">选择单位<text bindtap="funPrivatehizzz" class="quantities-complaintModal-content-close">x</text>
                    </view>
                    <view class="quantities-complaintModal-content-box">
                        <view class="{{v.$original.click ? 'quantities-complaintModal-content-box-list-click' : 'quantities-complaintModal-content-box-list'}}" wx:key="id" bindtap="anonymousFunc0" wx:for="{{loopArray40}}" wx:for-item="v" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey}}">{{v.$original.name}}</view>
                    </view>
                </view>
            </view>
        </block>
    </view>
</block>