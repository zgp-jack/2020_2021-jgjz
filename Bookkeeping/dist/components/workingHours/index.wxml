<block wx:if="{{$taroCompReady}}">
    <view>
        <block wx:if="{{display}}">
            <view class="workingHours-complaintModal">
                <view class="workingHours-complaintModal-content">
                    <view class="workingHours-complaintModal-content-title">选择上班时间<text bindtap="funPrivatehizzz" class="workingHours-complaintModal-content-close">x</text>
                    </view>
                    <view class="workingHours-complaintModal-content-timeBox">
                        <view class="workingHours-complaintModal-content-box">
                            <view class="{{v.$original.click ? 'workingHours-complaintModal-content-box-list-click' : 'workingHours-complaintModal-content-box-list'}}" wx:key="id" bindtap="anonymousFunc0" wx:for="{{loopArray39}}" wx:for-item="v" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{v._$indexKey}}">{{v.$original.name}}</view>
                        </view>
                    </view>
                </view>
            </view>
        </block>
    </view>
</block>