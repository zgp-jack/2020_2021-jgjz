<block wx:if="{{$taroCompReady}}">
    <view>
        <block wx:if="{{display}}">
            <view class="content">
                <view class="imgBox">
                    <view class="img">
                        <image src="{{IMGCDNURL + 'userauth-icon.png'}}" class="img-img"></image>
                    </view>
                    <view class="name">记工记账</view>
                    <view class="text">手机记工，一目了然，记工数据，清清楚楚、明明白白</view>
                    <view class="btn">
                        <block wx:if="{{!warrant}}">
                            <button class="sign" open-type="getUserInfo" bindgetuserinfo="anonymousFunc0">微信授权登录</button>
                        </block>
                        <block wx:if="{{warrant}}">
                            <button class="sign" bindtap="anonymousFunc1">微信快捷登陆</button>
                        </block>
                        <block wx:if="{{!warrant}}">
                            <view class="close" bindtap="anonymousFunc2">取消</view>
                        </block>
                        <block wx:if="{{warrant}}">
                            <view class="close" bindtap="anonymousFunc3">手机号登陆</view>
                        </block>
                    </view>
                </view>
            </view>
        </block>
    </view>
</block>